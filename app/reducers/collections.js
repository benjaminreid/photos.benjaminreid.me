const initialState = [
  {
    id: 1,
    name: 'Bristol'
  }, {
    id: 2,
    name: 'Leigh Woods'
  }
];

const store = function(state = initialState, action) {
  return state;
};

export default store;
